### Install K3S cluster on the VM

Ansible should have retreive the configuration (k3s.yaml) of the kubernetes cluster running on the VM.   
This change must be applie to the config: https://127.0.0.1:6443 -> https://REAL_CLOUD_IP:6443
