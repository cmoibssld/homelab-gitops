apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: focalb-demo
spec:
  accessModes:
    - ReadWriteOnce
  # storageClassName: local-path # aka default
  resources:
    requests:
      storage: 1Gi
